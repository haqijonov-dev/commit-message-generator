// Tailwind v4 PostCSS pluginini ulaydi.
// Next.js CSS faylllarini build paytida shu orqali tahlil qiladi.

const config = {
  plugins: {
    '@tailwindcss/postcss': {},
  },
}

export default config
